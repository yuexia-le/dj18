name: K8s-Edition CI/CD

on:
  push:
    branches: [ "main" ]

jobs:
  # 第一阶段：云端构建 (CI) - 使用 GitHub 托管服务器
  build:
    # 保留你的判断：只有合入 main 才会执行（或者 PR 针对 main 触发）
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest  # 使用 GitHub-hosted 节省本地资源
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Aliyun Registry
        uses: docker/login-action@v3
        with:
          registry: crpi-8tpi8vcb6euhian5.cn-shenzhen.personal.cr.aliyuncs.com
          username: ${{ secrets.ALIYUN_REGISTRY_USER }}
          password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          provenance: false
          tags: |
            crpi-8tpi8vcb6euhian5.cn-shenzhen.personal.cr.aliyuncs.com/dsl_taiji/vocab_master:latest
            crpi-8tpi8vcb6euhian5.cn-shenzhen.personal.cr.aliyuncs.com/dsl_taiji/vocab_master:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=inline

  # 第二阶段：本地部署 (CD) - 使用客户虚拟机 Runner
  deploy:
    needs: build
    runs-on: self-hosted 
    steps:
      # 1. 注入钥匙 (核心安全步骤)
      - name: Configure Kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.KUBE_CONFIG }}" > $HOME/.kube/config
          chmod 600 $HOME/.kube/config

      # 2. 拉取镜像并注入 Kind (关联本地环境)
      - name: Load Image to Kind
        run: |
          docker login --username=${{ secrets.ALIYUN_REGISTRY_USER }} --password=${{ secrets.ALIYUN_REGISTRY_PASSWORD }} crpi-8tpi8vcb6euhian5.cn-shenzhen.personal.cr.aliyuncs.com
          docker pull crpi-8tpi8vcb6euhian5.cn-shenzhen.personal.cr.aliyuncs.com/dsl_taiji/vocab_master:latest
          kind load docker-image crpi-8tpi8vcb6euhian5.cn-shenzhen.personal.cr.aliyuncs.com/dsl_taiji/vocab_master:latest

      # 3. 滚动更新 K8s Deployment (模拟真实发布)
      - name: Update K8s Deployment
        run: |
          # 强制更新镜像，K8s 会自动启动新 Pod 并销毁旧 Pod，实现不停机更新
          kubectl set image deployment/vocab-app vocab-app=crpi-8tpi8vcb6euhian5.cn-shenzhen.personal.cr.aliyuncs.com/dsl_taiji/vocab_master:latest -n vocab-pro
          
          # 等待滚动更新完成，如果新版本崩溃，此命令会报错并标记部署失败
          kubectl rollout status deployment/vocab-app -n vocab-pro

      # 4. 清理宿主机冗余镜像
      - name: Cleanup
        run: docker image prune -f